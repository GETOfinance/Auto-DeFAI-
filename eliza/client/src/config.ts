// This simplified config file is for the standalone chat application
export const API_ENDPOINTS = {
    // This is a placeholder for future backend integration
    // Right now our app works completely client-side
};